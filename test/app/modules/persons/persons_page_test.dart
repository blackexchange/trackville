import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:trackville/app/modules/persons/persons_page.dart';

void main() {
  testWidgets('PersonsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PersonsPage(title: 'Persons')));
    //  final titleFinder = find.text('Persons');
    //  expect(titleFinder, findsOneWidget);
  });
}
